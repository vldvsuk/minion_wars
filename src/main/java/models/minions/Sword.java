package models.minions;
/**Verschillende klassen die worden niet gebruikt, staan nog omdat ik wil deze project nog later uitbreiden**/
public class Sword extends Minion {
    public Sword(String type, String name, int cost, int movement, int minRange, int maxRange, int attack, int defence, String effect, int effectValue) {
        super(type, name, cost, movement, minRange,maxRange, attack, defence,effect, effectValue);
    }
}
