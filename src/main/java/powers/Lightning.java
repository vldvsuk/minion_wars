package powers;

public class Lightning extends Power {
    public Lightning(int radius, int value, String effect, int effectValue) {
        super("lightning", radius, value, effect, effectValue);
    }
}
